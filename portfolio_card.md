# 作品集卡片更新代码

把你作品集（github.com/2-creat/2-creat.github.io）里「机器视觉 Demo」那张卡片，
替换为下面这段（链接已指向新仓库 `aoi-demo`，描述也更新了）：

```markdown
[![机器视觉 Demo 封面：工件尺寸测量结果图](assets/covers/vision-demo.png) 已完成](https://github.com/2-creat/aoi-demo)

### 机器视觉 Demo

OpenCV 相机标定（尺子参考，ppm 换算）+ 工件尺寸测量 + 中值滤波模板差分缺陷检测；真实照片验证（红色块 53.5×64.3mm，判定 OK），完整跑通 AOI 视觉流程。

Python · OpenCV · 机器视觉 · AOI
```

如果你作品集里还没有这张卡片，直接加在 `#projects` 区块、现有卡片之后即可，结构完全一致。
